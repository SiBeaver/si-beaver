{
  "name": "si-beaver",
  "version": "0.2.0",
  "private": true,
  "description": "AI-native cognitive engine — monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm --filter @si-beaver/server test"
  },
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
