{
  "name": "@si-beaver/sibs",
  "version": "0.2.0",
  "private": true,
  "description": "REST API server + workflow orchestration",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "if [ -z \"$SKIP_DTS\" ]; then tsc; fi && tsup",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit"
  },
  "license": "AGPL-3.0-only",
  "dependencies": {
    "@hono/node-server": "^2.0.4",
    "@si-beaver/server": "workspace:*",
    "hono": "^4.7.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
